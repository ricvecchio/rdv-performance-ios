import SwiftUI

// Wrapper simples para a feature de mapa demo
struct MapFeatureView: View {
    var body: some View {
        MapDemoView()
    }
}

struct MapFeatureView_Previews: PreviewProvider {
    static var previews: some View {
        MapFeatureView()
    }
}
