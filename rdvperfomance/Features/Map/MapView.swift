import SwiftUI

// Wrapper para a funcionalidade de mapa de demonstração
struct MapFeatureView: View {
    // Retorna a view de demonstração do mapa
    var body: some View {
        MapDemoView()
    }
}

struct MapFeatureView_Previews: PreviewProvider {
    static var previews: some View {
        MapFeatureView()
    }
}
